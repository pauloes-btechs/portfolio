# Copy to .env.local for local dev; set these in Vercel → Project → Settings → Environment Variables
RESEND_API_KEY=re_xxxxxxxxxxxx
CONTACT_TO=pauloes@btechs.io
# The "from" must be on a domain you verify in Resend (Settings → Domains)
CONTACT_FROM=Portfolio <hello@pauloes.com>
